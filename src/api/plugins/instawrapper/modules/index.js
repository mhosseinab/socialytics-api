
const GetUserInfo = require('./GetUserInfo');
const GetUserMedia = require('./GetUserMedia');
const GetMediaComments = require('./GetMediaComments');
const GetCommentThread = require('./GetCommentThread');

module.exports = {
  GetUserInfo,
  GetUserMedia,
  GetMediaComments,
  GetCommentThread,
}
